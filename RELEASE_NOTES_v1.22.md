# DearScene v1.22 Beta

v1.22は、v1.21の大規模ライブラリ対応を保ったまま、配布物と外部ヘルパーの信頼境界を固めるセキュリティ更新です。

## 主な変更

- 内蔵FFmpegのSHA-256をビルド時、展開時、起動前に照合。同サイズのファイルへ差し替えられても実行せず、正規コピーへ自動復元
- 配布版では環境変数、アプリ直下、PATH上のFFmpegを採用せず、検証済み内蔵版だけを利用
- FFmpeg入力をローカルファイルとアプリ内パイプへ限定し、ネットワークプロトコルを遮断
- シェーダーコンパイラDLLをWindows System32からのみ読み込むよう制限
- RAW内蔵プレビューの読込上限を128MiBとし、異常なサイズ宣言による過大メモリ確保を防止
- 同一ドライブの読込みを画像・動画別の回復可能な複数スロットへ分け、6窓同時利用でも1窓がI/O経路を占有しないよう調整
- 動画デコーダーを1窓4基に固定。枠が埋まった場合や動画が5秒で開かない場合は、黒画面や停止を待たずローカル生成ポスターへ迂回
- 実行マニフェストの製品ID、asInvoker権限、Windows 10/11互換情報と、ファイルの会社・著作権情報を整備
- ベータ利用条件、セキュリティ報告手順、SmartScreen・ハッシュ確認・完全削除の案内を追加

## 互換性

- 対象: Windows 10/11 x64
- 本体はネットワーク通信を行いません。
- 元の写真・動画は移動、編集、削除しません。
