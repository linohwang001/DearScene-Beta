# DearScene v1.21 Beta

公開基準日: 2026-08-23

96万件規模のライブラリでも、初回走査中と表示レイアウト変更中に操作を止めないための性能修正版です。

## 大規模ライブラリ

- フォルダー走査を全件取得・並べ替えから逐次列挙へ変更し、見つかった項目から再生準備を開始
- 走査結果を512件単位で一括登録し、1ファイルごとのUI通知を廃止
- 永続索引と実フォルダー走査を並行利用しつつ、重複候補を一括で除外
- 96万件の候補登録を一括処理し、フォルダー順の並べ替えを候補ごとではなくバッチごとに実行

## レイアウト変更

- メイソンリー、短冊、分割数の変更時は候補プールをバックグラウンドで再構築
- 再構築中も現在の写真・動画と設定画面を保持し、操作を継続可能
- 変更を連続操作した場合は古い再構築を中止し、最後に選んだ設定だけを適用
- 旧先読みワーカーの終了待ちをUIスレッドから分離

## 定数時間化

- メイソンリーの写真優先検索を、全件走査から最大128候補の探索へ変更
- ページ補助画像を全カタログ複製から最大84候補のサンプリングへ変更
- 安全在庫とプレビュー数の確認を、最大512件の退避候補と最大72件のキャッシュへ限定
- ランダム／おすすめ再生で候補を取り出す際、残り全件の配列移動を廃止

## 検証

- 960,000候補の一括構築、写真優先選択、ページ候補抽出: PASS
- 従来設定368,640通り: PASS
- 表示レイアウト221,184通り × 5更新: PASS
- 既存の実画面、全画面、メイソンリー、短冊、動画、フェイルセーフ試験: PASS
